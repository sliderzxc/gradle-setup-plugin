package com.sliderzxc.gradle.localization.core.config

/**
 * Enum class representing supported localization languages.
 *
 * @param code Language code associated with the language.
 */
enum class LocalizationLanguage(val code: String) {
    Ukrainian("uk"),
    Russian("ru")
}